{
  "name": "automationplaywrightcourse",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test tests/WebAPIPart1.spec.js --headed",
    "regression": "npx playwright test",
    "webTests": "npx playwright test --grep @Web",
    "apiTests": "npx playwright test --grep @API",
    "mobileTests": "npx playwright test --grep @Mobile",
    "SafariTests": "npx playwright test --project=safari",
    "chromeTests": "npx playwright test --project=chromium",
    "firefoxTests": "npx playwright test --project=firefox"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.2.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
